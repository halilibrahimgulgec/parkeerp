const { createClient } = require(" @supabase/supabase-js\); require(\dotenv\).config({ path: \.env.local\ }); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); supabase.from(\customers\).select(\*\).then(({data:cust}) => { const target = cust.find(c => c.name.includes(\ONİKİŞUBAT\)); supabase.from(\pallet_transactions\).select(\*\).eq(\customer_id\, target.id).then(({data:trans}) => console.log(JSON.stringify(trans, null, 2))); });